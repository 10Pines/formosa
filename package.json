{
  "name": "formines",
  "version": "0.1.1",
  "description": "Simple and composable validations for react",
  "url" : "https://github.com/10Pines/formines",
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "publish": "lerna publish"
  },
  "jest": {
    "rootDir": "lib"
  },
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "lerna": "^2.5.1"
  }
}
