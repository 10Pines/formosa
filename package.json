{
  "name": "formosa",
  "version": "0.0.0",
  "description": "Simple and composable validations for react",
  "url": "https://github.com/10Pines/formosa",
  "scripts": {
    "setup": "lerna bootstrap",
    "clean": "lerna run clean",
    "test": "lerna run test",
    "build": "lerna run build",
    "publish": "run-s clean test build publish-raw",
    "publish-raw": "lerna publish"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^2.5.1",
    "npm-run-all": "^4.1.3"
  },
  "dependencies": {
    "generator-ngx-library": "^6.2.1",
    "yo": "^2.0.5"
  }
}
